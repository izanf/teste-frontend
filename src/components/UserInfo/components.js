import styled from 'styled-components';

const Wrapper = styled.div`

`;

const Picture = styled.img`
width: 32px;
height: 32px;
object-fit: cover;
border-radius: 100%;
`;

export default {
  Wrapper,
  Picture,
};
